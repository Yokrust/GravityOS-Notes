# Demo Context

Gravity is local-first and folder-system-first.

This demo exists to show that:

- the root `AGENTS.md` shapes the run,
- local context can narrow the task,
- outputs belong in a visible output area.
