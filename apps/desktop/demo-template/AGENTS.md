# Gravity Demo Map

Do your first run within this Folder System.

Create a small `welcome.md` artifact in the `output` folder that:

- explains how this demo is structured,
- describes how the map and local context shaped the run,
- ends with two follow-up questions the user can answer next.
