# Triage Labels

The skills speak in terms of five canonical triage roles. This file maps those roles to the actual label strings used in this repo's issue tracker.

| Label in mattpocock/skills | Label in our tracker | Meaning                                  |
| -------------------------- | -------------------- | ---------------------------------------- |
| `needs-triage`             | `Triage`             | Maintainer needs to evaluate this issue  |
| `needs-info`               | `Info`               | Waiting on reporter for more information |
| `ready-for-agent`          | `Ready for Agent`    | Fully specified, ready for an AFK agent  |
| `ready-for-human`          | `Human`              | Requires human implementation or HITL    |
| `wontfix`                  | `Skip`               | Will not be actioned                     |

When a skill mentions a role, use the corresponding Linear label from this table.
